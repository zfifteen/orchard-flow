# Short smoke configuration for tests and local sanity checks.
nx = 32
ny = 32
reynolds = 100
lid_velocity = 1.0
cfl_limit = 0.5
max_steps = 12
min_steps = 1
steady_tolerance = 1e-12
poisson_max_iterations = 200
poisson_tolerance = 1e-10
validate_reference = false
